#include <stdio.h>
#include <stdlib.h>

// Imprime una cádena de caracteres con el cifrado de Julio César, donde cada caracter es incrementado en 3. De manera que, la 'a' pasaría a ser la 'd' y, la 'z' la 'c' .

int main(int argc, char *argv[]){

	char palabra[] = {"abcd"};
	system("clear");

	printf("\n %s", palabra);

	printf("\n\n");
	return EXIT_SUCCESS;
}
